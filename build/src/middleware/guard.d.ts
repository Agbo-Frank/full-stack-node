import { NextFunction, Response } from "express";
export default function guard(req: any, res: Response, next: NextFunction): void;
export declare const guardValid: import("express-validator").ValidationChain;
